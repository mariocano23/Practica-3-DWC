"use strict";

function numerosPrimYPali(params) {
    let primo = false;
        for (var i = 2; i < num || ; i++) {
            if (num%i==0){
                primo = false;
            }else{
                primo = true;
                break;
            }
        };
        
    
}